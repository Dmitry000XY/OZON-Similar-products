# Raw Data

Исходные данные. Архивы хранятся в `archives/`, распакованный справочник товаров - в `product_information/`, действия пользователей - в `user_actions/`.

Для подготовки из архивов в корне проекта используйте `uv run python scripts/prepare_raw_data.py`.
